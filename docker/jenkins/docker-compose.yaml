version: '3.1'

services: 

  jenkins:
    image: jenkins/jenkins:lts-jdk11
    restart: always
    networks:
      - network_bridge
    volumes:
      - $PWD/jenkins_home:/var/jenkins_home
    ports:
      - 8080:8080
      - 50000:50000

networks:
  network_bridge:
    driver: bridge